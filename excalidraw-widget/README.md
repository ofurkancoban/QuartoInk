# Excalidraw bundle for Quarto Ink

Build the browser bundle once (or after dependency updates):

```bash
cd excalidraw-widget
npm install
npm run build
```

Output: `dist/quarto-ink-excalidraw.js` (loaded by `../index.html`).
